# d10 agent status: C^4 (x) C^5 (x) C^5, rank 8 vs rank 9 (isotypic flattenings, all degrees in the job queue)
Updated 2026-09-24 20:07 UTC. Work is pushed to the shared branch `claude/wonderful-fermat-1v76ux` (mirror `claude/tender-hopper-mcnmta`).
Setting: T in C^4 (x) C^5 (x) C^5 (generic rank 9, sigma_8 of codimension 3). "Separates" = smaller flattening rank on a random rank-8
tensor than on a random rank-9 tensor. Tool: `sweep_fast.py 4,5,5 d 8,9 5 <k> 3 noV` (d9 agent's Cauchy-Binet / float64-BLAS evaluator,
generalised to different factor dimensions; exact mod p = 524287; components = (l1; {l2,l3}) unordered in the two C^5 factors, all with
Kronecker coefficient g > 0; three flattening directions; gN = generic functional, HN = full multiplicity space stacked). 3 workers, seed 5.

## Running now
    (no sweep process running)

## Jobs (t455_jobs.txt) and progress per degree
| degree | components (g>0) | checked | hits | sum of times (h) | slowest (s) |
|---|---|---|---|---|---|
| 2 | 3 | 3 | 0 | 0.00 | 0 |
| 3 | 8 | 0 | 0 | 0.00 | 0 |
| 4 | 28 | 0 | 0 | 0.00 | 0 |
| 5 | 82 | 0 | 0 | 0.00 | 0 |
| 6 | 250 | 0 | 0 | 0.00 | 0 |
| 7 | 639 | 0 | 0 | 0.00 | 0 |

Sum of per-component times so far: 0.00 h (3 workers in parallel). Jobs: d=2, d=3, d=4, d=5, d=6, d=7.

## Hits (`*** SEPARATES`)
None so far: every checked flattening has the same rank on the random rank-8 and rank-9 tensors.

## Method notes / [method] commits by this agent
* 2026-09-24 `[method]` hwv.py: optimal contraction path (opt_einsum DP minimising the largest intermediate) + MEMCAP batch splitting for the
  int64 evaluator (adopted by d7d8 as b3feecb). Superseded for sweeps by the d9 agent's hwv_fast/sweep_fast (be0ecaf), which this agent adopted.
* 2026-09-24 `[method]` mixed factor dimensions: `random_gs` (hwv.py), `prepare` (hwv_fast.py: minors up to each factor's own dimension,
  only for the column lengths that occur), `sweep_fast.py`/`test_fast.py` take `n1,n2,n3`. Equal-dimension runs are unchanged (d=4 ranks identical
  to hwv4_d4.log via both `4` and `4,4,4`; C^3 d=5 control finds exactly the 3 known separators). Mixed-dimension checks: fast == reference
  `hwv.flattening_matrix` entrywise on 16 (4,5,5) cases with 5-row partitions and r=9; ranks in (4,5,5) equal the ranks computed in (5,5,5) on the
  same tensor embedded with a zero coordinate (test_embed, d=5, r=9). Reference int64 code is 3-45x slower per flattening at r=9 (test_fast).

## Restart (fresh container, from the pushed logs alone)
```
git clone -b claude/tender-hopper-mcnmta https://github.com/wajcha2/claude && cd claude      # sweep dir (own branch; any branch with the code works)
git worktree add /home/user/claude-shared claude/wonderful-fermat-1v76ux                   # used only to pull --rebase / push the shared branch
cd isotypic_flattenings && cp /home/user/claude-shared/isotypic_flattenings/hwv455_*.log . 2>/dev/null   # authoritative logs = shared branch
pip install numpy sympy opt_einsum
bash t455_checkin.sh      # restarts the workers of the first unfinished job in t455_jobs.txt with RESUME=<all hwv455_d<d> logs>, or starts the next job
# by hand: RESUME=$(ls hwv455_d<d>*.log | tr "\n" ":") MAXDIM=<inf|N> CLAIMDIR=claims455_d<d>[_max<N>] OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
#   setsid nohup python3 sweep_fast.py 4,5,5 <d> 8,9 5 <k> 3 noV >> hwv455_d<d>[_max<N>]_w<k>.log 2>&1 < /dev/null &     (k = 0,1,2; remove the dead worker's claim files first)
```
A worker is finished when the last line of its log is `n=4,5,5 d=... FOUND: [...]`. Hourly check-in routine trig_01Kq52M65tDvcZNxEXAWNoxm (this session).

## Degree 2: components checked (3 of 3)
gN/HN = generic functional / full-multiplicity-space stack in direction N (1 = source S^{l1}(C^4)^*, 2 = S^{l2}(C^5)^*, 3 = S^{l3}(C^5)^*); ranks rank-8/rank-9.

| lam | g | dims | ranks | span | s |
|---|---|---|---|---|---|
| ((2,), (2,), (2,)) | 1 | (10, 15, 15) | g1:10/10 g2:15/15 g3:15/15 | 1,1,1 | 0 |
| ((2,), (1, 1), (1, 1)) | 1 | (10, 10, 10) | g1:10/10 g2:10/10 g3:10/10 | 1,1,1 | 0 |
| ((1, 1), (2,), (1, 1)) | 1 | (6, 15, 10) | g1:6/6 g2:15/15 g3:10/10 | 1,1,1 | 0 |

## Previous task of this agent: C^4 (x) C^4 (x) C^4, d = 10, rank 6 vs 7 (stopped 2026-09-24 19:42 UTC)
Stopped by the user after the d9 agent found two degree-9 separators. 19 of the 279 pass-1 components (largest Weyl dimension <= 150) were
checked with no separator (logs `hwv4_d10_*.log`, per-component ranks in the git history of agents/d10.md before this task). Restart: `D10_RESUME=1 bash d10_checkin.sh`.
