# d9 agent status (C^4 (x) C^4 (x) C^4, rank 6 vs 7, degree d = 9)
Updated 2026-09-24 19:58 UTC. Session https://claude.ai/code/session_013ykGGDZCS3NweFgqZwxY4P.
Pushes to the shared branch `claude/wonderful-fermat-1v76ux` (mirrored on `claude/clever-newton-rism5d`).

## HITS: two degree-9 isotypic flattenings separate rank 6 from rank 7 (verified)
Both are g = 1 components (the functional is unique up to scale), direction 1 (source = first partition):

| component lam | flattening | dims | matrix | rank 5 | rank 6 | rank 7 | rank 8 |
|---|---|---|---|---|---|---|---|
| ((8,1),(3,3,3),(3,3,2,1)) | S^{81}V^* -> S^{333}V (x) S^{3321}V | 440 -> 20*20 = 400 | 444 x 444 | 210 | **396** | 400 | 400 |
| ((7,2),(4,2,2,1),(3,3,3)) | S^{72}V^* -> S^{4221}V (x) S^{333}V | 540 -> 36*20 = 720 | 544 x 544 | 235 | **500** | 504 | 504 |

* Sweep (seed 5, p = 524287): g1 = 396/400 and 500/504; the other two directions have equal (full) ranks.
* Verification (`verify_hit.py`, output in `verify4_d9_hits.log`): three independent draws of the tensors
  (ranks 5, 6, 7, 8), of the N1 = K = n1 + 4 evaluation points and of the filling, with two primes (524287 twice,
  524269 once): identical ranks every time. An 8 x 8 block of the flattening matrix equals the reference
  implementation `hwv.flattening_matrix` entry by entry (both primes).
* Reference check (`ref_check.py`, reference implementation `hwv.flattening_matrix` only, full 444 x 444 matrix,
  fresh random data): hit 1 gives 396 (rank 6) / 400 (rank 7). Hit 2: running.
* 2x2 matrix multiplication M2 (border rank 7), `special_tensor.py`, log `special4_d9_hits.log`: flattening rank
  20 for hit 1 and 36 for hit 2, identical for the 8-term decomposition, Strassen's 7 terms and a random
  GL4^3 translate (both primes). So the equations vanish at M2: they do NOT reprove border rank(M2) = 7. The rank-4
  unit tensor also gives 20, and random rank-5 tensors give 210 / 235.
* Consequence: the 400 x 400 minors of the first flattening (504 x 504 minors of the second; entries of degree 9 in
  T) vanish on sigma_6 and not on a general tensor. Precisely: rank <= 396 on sigma_6, so the 397 x 397 minors
  (degree 9*397 = 3573) vanish on sigma_6. For hit 2 the 501 x 501 minors (degree 4509) vanish on sigma_6. For the first one the general rank is the full target dimension
  400 (S^{333}V = S^3V^* (x) det^3, S^{3321}V = S^{221}V (x) det), for the second one it is below both dimensions.
  In both cases the drop on sigma_6 is 4 = codim sigma_6.

## Running
* `sweep_fast.py 4 9 6,7 5 <k> 3 noV`, k = 0,1,2 (3 workers, dynamic claiming, cheapest components first by the cost
  proxy g * sum_i (n_i + 4)^2). Started 19:24 UTC, restarted 19:34 UTC on the float64 version of [method] a1b29ba.
* Progress at 19:43 UTC: 465 / 782 components; 556 / 782 at 19:49 (0.9 h of compute; per component median 6.3 s, 90% below 12.8 s,
  max 23.6 s so far; later components are larger). Table: `hwv4_d9_summary.md` (regenerated at every push);
  logs `hwv4_d9_s{0,1,2}.log` (snapshots of the live logs).
* Memory: < 0.2 GB per worker so far (MEMCAP = 2^25 elements, blocks otherwise).

## Restart (fresh container)
```
pip install numpy sympy opt_einsum
cd isotypic_flattenings; mkdir -p claims_d9
for k in 0 1 2; do PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  RESUME=hwv4_d9_s0.log:hwv4_d9_s1.log:hwv4_d9_s2.log CLAIMDIR=claims_d9 \
  nohup python3 sweep_fast.py 4 9 6,7 5 $k 3 noV >> hwv4_d9_s$k.log 2>&1 & done
```
(Components already in the logs are skipped; remove `claims_d9` before a restart. OPENBLAS_NUM_THREADS=1 matters:
one BLAS thread per worker.)

## [method] a1b29ba: hwv_fast.py + sweep_fast.py (30-300x faster, bit-identical matrices)
* Idea 1 (Cauchy-Binet): a column determinant det(V_{s_c} g[:, :|c|]) equals sum_I det g[I, :|c|] * det V[s_c, I] over
  |c|-subsets I of [n]. In the tensor network every column op (batch x r^|c|) becomes two small ops joined by a bond
  of size C(n,|c|) <= 6 (a length-4 column becomes det(g) * det(V)). The path optimiser can then route the
  contraction through Lambda^{|c|} C^4 instead of r^|c| word indices.
* Idea 2: flop-minimising path (opt_einsum RandomGreedy, 128 tries) instead of the size-greedy order.
* Idea 3: exact float64 BLAS. All tensors hold residues 0..p-1 as float64; products are < p^2 < 2^38, so dot
  products of <= 2^15 terms are exact (< 2^53); reduce with fmod. numpy's int64 matmul has no BLAS, so it was the
  bottleneck (15 of 17 s in a profile).
* Measured (single flattening, same inputs, old int64 code -> new): d=9 ((9),(9),(9)) 16 s -> 0.2 s;
  d=8 ((4,3,1),(3,3,1,1),(3,2,2,1)) 130 s / 67 s -> 0.4 s; d=7 ((4,2),(3,2,1),(3,1,1,1)) 12-13 s -> 0.2-0.4 s.
  Whole sweeps: C^4 d=6 29 min -> 83 s with identical ranks (345 fields vs hwv4_d6.log; d=4, d=5 identical too);
  C^3 d=5 positive control finds exactly the three known separators (3/6, 12/15, 12/15 and V1 15/21).
* sweep_fast.py, differences from sweep_hwv.py: exactly g fillings whose functionals are independent (checked on a
  probe) instead of 2g+2 unchecked ones, so gN is a generic functional and HN is the full-M flattening. Cheapest
  fillings are used first. HN is compressed by a random matrix with 8 extra columns before `modrank`.
* Not yet compared with the d10 DP-path version of hwv.py (b3feecb); hwv_fast only imports `p` and `col_dets` from
  hwv.py, which that commit leaves unchanged.
